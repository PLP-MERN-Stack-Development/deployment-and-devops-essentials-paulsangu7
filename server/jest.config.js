module.exports = {
  testEnvironment: 'node',          // Node.js environment for backend
  testMatch: ['**/tests/**/*.test.js'], // Match your backend test files
  verbose: true,
  collectCoverage: true,
  coverageDirectory: 'coverage'
};
